{
  "model": "GPT-5.6 Sol",
  "experiment": "orlib-binpack1-recorded-llm-proposals",
  "notes": "Generation 1 proposed before results. Generation 2 conditioned only on generation-1 train/validation evidence: a 0.10-capacity threshold rule led training, while Best Fit remained validation-selected. Test was not used to construct generation 2.",
  "generations": [
    [
      "(2.0 - residual / capacity) if residual < 0.10 * capacity else remaining / capacity",
      "(2.0 + fill_ratio) if residual < 0.15 * capacity else residual / capacity",
      "tightness + 0.25 * fill_ratio - 0.05 * item_ratio",
      "-residual + 0.10 * remaining - 0.05 * open_bins",
      "tanh(3.0 * tightness) - 0.10 * residual / capacity",
      "(3.0 - residual / capacity) if item_ratio > 0.5 else remaining / capacity",
      "tightness + 0.20 * item_ratio + 0.05 * step_fraction",
      "-(residual ** 2) / (capacity ** 2) + 0.15 * fill_ratio",
      "(2.0 + tightness) if residual < item else remaining / capacity",
      "max(tightness, fill_ratio) - 0.05 * open_bins",
      "tightness + 0.10 * tanh(open_bins / 10.0)",
      "-abs(residual) / capacity + 0.20 * fill_ratio",
      "(2.5 + fill_ratio) if residual < 0.20 * capacity else -residual / capacity",
      "tightness + 0.15 * (item_ratio ** 2) - 0.05 * step_fraction",
      "-residual / capacity + 0.10 * (fill_ratio ** 2)",
      "(3.0 - residual / capacity) if residual < 0.25 * capacity else fill_ratio",
      "tightness - 0.10 * item_ratio * residual / capacity",
      "max(-residual / capacity, fill_ratio - 0.5)",
      "tightness + 0.10 * log1p(open_bins)",
      "(2.0 + item_ratio) if residual < 0.12 * capacity else remaining / capacity",
      "tightness + 0.25 * fill_ratio * item_ratio",
      "-(residual / capacity) ** 2 + 0.05 * open_bins",
      "(2.0 + tightness) if fill_ratio > 0.5 else remaining / capacity",
      "tightness + 0.05 * step_fraction - 0.02 * open_bins"
    ],
    [
      "(2.0 - residual / capacity) if residual < 0.05 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.075 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.125 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.15 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.175 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.20 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.25 * capacity else remaining / capacity",
      "(3.0 - residual / capacity) if residual < 0.10 * capacity else remaining / capacity",
      "(2.0 + fill_ratio) if residual < 0.10 * capacity else remaining / capacity",
      "(2.0 + tightness) if residual < 0.10 * capacity else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.10 * capacity else residual / capacity",
      "(2.0 - residual / capacity) if residual < 0.10 * capacity else remaining / capacity - 0.10 * item_ratio",
      "(2.0 - residual / capacity) if residual < 0.10 * capacity else remaining / capacity + 0.05 * step_fraction",
      "(2.0 - residual / capacity) if residual < 0.10 * capacity else remaining / capacity - 0.02 * open_bins",
      "(2.0 + tightness) if residual < 0.50 * item else remaining / capacity",
      "(2.0 + tightness) if residual < 0.25 * item else remaining / capacity",
      "(2.0 + tightness) if residual < 0.20 * capacity + 0.10 * item else remaining / capacity",
      "(2.0 + tightness) if residual < 0.08 * capacity + 0.10 * item else remaining / capacity",
      "(2.0 - residual / capacity) if residual < 0.10 * capacity else residual / capacity",
      "(3.0 - residual / capacity) if residual < 0.12 * capacity else residual / capacity",
      "(2.0 + tightness) if residual < 0.12 * capacity else residual / capacity + 0.10 * fill_ratio",
      "(2.0 + tightness) if residual < 0.08 * capacity else residual / capacity",
      "(2.0 + tightness) if residual < 0.16 * capacity else residual / capacity",
      "(2.0 + tightness) if residual < 0.10 * capacity else residual / capacity + 0.05 * item_ratio"
    ]
  ]
}
